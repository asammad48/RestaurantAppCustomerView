# Environment Configuration for Restaurant Ordering App
# =====================================================

# Set the application environment
# Options: development, qa, production
# Default: development
# Note: Replit automatically uses development environment
VITE_APP_ENV=development

# Optional: Override API base URL (takes priority over environment config)
# Use this to point to a custom API endpoint if needed
# VITE_API_BASE_URL=https://your-custom-api-url.com

# Optional: Override SignalR Hub URL (takes priority over environment config)
# Use this to point to a custom SignalR endpoint if needed
# VITE_SIGNALR_HUB_URL=wss://your-custom-signalr-url.com/orderHub

# Production environment URLs (required for production deployment)
# VITE_PRODUCTION_API_URL=https://your-production-api-url.com
# VITE_PRODUCTION_SIGNALR_URL=wss://your-production-signalr-url.com/orderHub

# Replit-specific environment variable (automatically set in Replit environment)
# VITE_REPLIT=true

# Environment URLs (configured in client/src/lib/config.ts):
# ========================================================
# Development: https://5dtrtpzg-7261.inc1.devtunnels.ms
# QA: https://restaurant-app-web-qa-001-eecdfsadcfgxevc9.centralindia-01.azurewebsites.net
# Production: (placeholder - uses QA URLs until production is configured)

# Usage Examples:
# ===============
# For local development: VITE_APP_ENV=development
# For QA testing: VITE_APP_ENV=qa
# For production: VITE_APP_ENV=production
# Custom API override: VITE_API_BASE_URL=https://localhost:3000